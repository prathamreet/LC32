// Simple helper for future formatting needs.
// Currently unused but in place for potential extension.
public class Utils {
    public static String formatMessage(String nickname, String message) {
        return nickname + ": " + message;
    }
}